function Home() {
    return(
        <nav className="navbar">
            <link to="/" className="nav-logo" onClick={() => setOpen(false)}>
                Logo
            </link>
        </nav>
    )
}
    